@using mn.Models
@model List<Danhsach>
<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
                Danh sách trường
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Danhsach" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Thêm
            </a>
        </div>
    </div>
</div>
<div class="table-responsive">
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
                Tên Trường
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td>@obj.Id</td>
                <td>@obj.University</td>
                <td class="text-center">
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Danhsach" asp-action="Edit" asp-route-ID="@obj.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i> Chỉnh Sửa
                        </a>
                        <a asp-controller="Danhsach" asp-action="Delete" asp-route-ID="@obj.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash3-fill"></i> Xóa
                        </a>
                    </div>
                </td>
            </tr>

        }
    </tbody>
</table>
</div>